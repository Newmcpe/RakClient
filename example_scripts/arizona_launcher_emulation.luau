-- Arizona launcher emulation — the easy way.
--
-- The whole CEF / validation / join-rewrite dance lives in the reusable `arizona` module (built
-- with our typed packet-builder sugar), so a real script is one line. See
-- arizona_launcher_emulation_classic.luau for the same thing spelled out byte by byte.

require("arizona")
